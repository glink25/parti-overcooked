{
  "name": "parti-overcooked",
  "private": true,
  "version": "3.0.1",
  "type": "module",
  "description": "Low-poly 3D co-op cooking chaos room for Parti (2-4 players)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node --test tests/client/*.test.mjs tests/flows/*.test.mjs tests/maps/*.test.mjs tests/mechanisms/*.test.mjs tests/world/*.test.mjs",
    "test:maps": "node --test tests/world/*.test.mjs tests/mechanisms/*.test.mjs tests/maps/*.test.mjs",
    "test:package": "node --test tests/package/*.test.mjs",
    "verify": "npm run build && npm test && npm run test:package"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.2.0"
  },
  "dependencies": {
    "three": "^0.171.0"
  }
}
