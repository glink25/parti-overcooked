{
  "name": "parti-overcooked",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "Low-poly 3D co-op cooking chaos room for Parti (2-4 players)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test:visual": "node --test src/client/visual/*.test.mjs",
    "test:accolades": "node --test src/worker/accolades.test.mjs",
    "verify": "npm run test:visual && npm run test:accolades && node verifier/v3/run.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.2.0"
  },
  "dependencies": {
    "three": "^0.171.0"
  }
}
